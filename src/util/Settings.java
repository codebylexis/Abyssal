package util;

/** Global constants. GRID_WIDTH/HEIGHT control tile snapping and placement in the editor (in world units). */
public class Settings {

	public static float GRID_WIDTH = 0.25f;
	public static float GRID_HEIGHT = 0.25f;
}
